# Changelog / 更新日志
## v1.9.2
- 手动模式说明改为“可手动勾选内容”。
- 智能过滤在手动模式下继续执行并展示疑似/排除状态，不再被“待选择”状态覆盖。
- 手动勾选结果在自动/手动模式之间切换时保留，切回手动模式会恢复之前选择。
- 修复站酷手动勾选图标闪烁问题。
- 站酷预览大图、右侧缩略图和作品正文改为共享同一素材 ID 与选择状态。
- 统一站酷 CDN 缩略图/大图的地址指纹，避免同图被误识别为多个素材。

## v1.9.1
- 修复花瓣 `/discovery` 首页被识别为未支持页面的问题。
- 修复首次安装后已打开页面没有自动注入、需要切换开关或刷新才能识别的问题。
- 合并花瓣启动设置读取，避免初始化竞态和重复扫描。

## v1.9.0
- 花瓣首页现在与 Pinterest 首页一致，可直接识别瀑布流、勾选并采集，无需先搜索。
- 新增全自动 / 手动选择模式开关，选择会持久保存并实时应用到当前页面。
- 手动模式下所有新识别内容默认处于待选择状态：绿色边框、空心图标；用户点击后显示实心绿色勾选。
- 手动选择模式统一支持花瓣、Pinterest、站酷、小红书和 Behance。

## v1.8.10
审美图采集助手 v1.8.10 完整功能清单：
### 小红书
- 支持笔记内普通图文、短视频、Live动态照片采集；仅笔记详情页展示采集框，列表页不激活
- 轮播每页素材独立识别，支持单张勾选/取消、一键全选；取消勾选可撤回待下载队列
- Live素材直接下载原始MP4动态文件，不会降级为静态图片
- 采集框定位稳定，滚动、切换轮播无重叠、飞框、残留错位问题
- 自动提取高清原始资源地址，剔除页面缩放与水印参数，保存原图画质

### Behance
- 作品详情正文素材识别，自动过滤作者头像、广告、推荐封面
- 支持图片、GIF、内嵌Vimeo视频采集
- 视频优先下载完整渐进式MP4；无法直连时自动解析HLS分片，支持最高画质选择并自动合并分片
- GIF保留原始动画格式下载

### 花瓣 / Pinterest / 站酷
- 花瓣、Pinterest：瀑布流可视化勾选，双层素材去重，自动过滤广告与付费素材，支持手动恢复被拦截素材
- 站酷：仅采集作品正文图像，自动屏蔽横幅广告、评论区、头像与推荐素材，兼容懒加载长图
- 全站点统一勾选交互，窗口缩放、页面滚动时采集框跟随素材实时更新

### 批量下载 & ZIP打包
- 多网站素材共用统一下载队列，支持分批采集、集中打包
- 文件自动规范命名，标记来源平台、采集时间与素材序号
- 下载实时百分比进度展示，下载失败素材保留在队列，支持重新尝试下载

### 通用交互
- 侧边面板统一展示选中素材数量、素材分类统计，提供插件总开关，关闭后所有采集UI立即隐藏
- 页面切换、弹窗预览自动管理采集框显隐，避免界面冲突
