# Changelog / 更新日志

## v1.8.10

- 小红书选框运行文件完整回退到 1.8.8，撤销 1.8.9 的顶部固定栏视觉裁切，Live、普通图片和视频选框逻辑不再叠加新改动。
- Behance Vimeo 保留渐进式 MP4 优先下载；未开放直连 MP4 时，新增 HLS 播放列表解析、最高画质选择和 fMP4 分片合并回退。
- Behance GIF、ZIP、待下载、去重与其他站点保持 1.8.9 不变。

## v1.8.9

- 插件更名为“审美图采集助手”，顶部不再重复显示版本号，底部入口精简为“问题反馈”。
- Behance 项目正文新增 Vimeo 识别，下载时实时解析播放器开放的最高清渐进式 MP4；未开放 MP4 的分离式流会保留在失败队列并给出原因。
- 小红书 fixed 选框被顶部固定栏遮挡时仅裁切被遮住的视觉部分，不改变媒体绑定、坐标和 Live 轮播逻辑。
- 下载与 ZIP 打包的实时百分比和进度条已存在，本版保持原实现不变。

## v1.8.8

- Live 勾选图标 hover 鼠标改为与普通图片、视频一致的手型，仍不执行 hover 放大。
- Live 页面或弹层滚动时立即隐藏 fixed 选框，不再用旧坐标跟随滚动；停止后按当前实际媒体区域重新定位。
- Behance 从项目详情元数据识别 `ImageModule` 中的 GIF 原始资源，保留动画并以 `.gif` 下载；`EmbedModule` 视频继续忽略。
- Live MP4、ZIP、普通图片、普通视频和其他站点逻辑保持 1.8.7 不变。

## v1.8.7

- 根据用户录屏修复 Live 选框依赖鼠标停留的问题：首次稳定后像普通图片一样固定显示，只在笔记或真实轮播页变化时换绑。
- Live 播放缩放、鼠标进入/离开及 `img`/`video` 临时替换不再触发选框隐藏；媒体节点短暂移除时保留当前页选框。
- Live 勾选图标不再夺走下方动态画面的鼠标命中，点击由坐标捕获处理，避免触发小红书 `mouseleave` 后频闪。
- Live MP4、Behance、ZIP、普通图片和普通视频逻辑保持 1.8.6 不变。

## v1.8.6

- 仅修复鼠标悬停 Live 勾选图标时选框频闪：Live 图标取消通用 hover 缩放，保持固定命中区域。
- 悬停期间锁定已经稳定的 Live 选框，隔离小红书自身悬停层引发的短暂媒体节点替换；离开图标后恢复扫描。
- Live 轮播绑定、MP4、Behance、ZIP、普通图片和普通视频逻辑保持 1.8.5 不变。

## v1.8.5

- 仅修复多页 Live 轮播选框：按封面 URL 预先把所有已加载宿主绑定到各自的 `slideIndex` 与 MP4 记录。
- Live 切页后优先选择当前激活宿主，再读取宿主内实际 `img`/`video`；页码仅用于校验和备用。
- 轮播移动期间隐藏 Live 选框，当前媒体稳定两帧后按实际绘制区域显示；`img` 切换为 `video` 时继承原记录。
- Live MP4、Behance、ZIP、普通图片和普通视频逻辑保持 1.8.4 不变。

## v1.8.4

- 根据用户录屏修复 Live 轮播后读到旧页码的问题：Live 页码必须位于当前视口内，且祖先节点未隐藏。
- 轮播结束后按真实可见页码重新绑定对应 Live 媒体记录并显示选框；普通图片/视频的原页码逻辑不变。
- Live MP4、Behance、ZIP 和其他站点代码保持 1.8.3 不变。

## v1.8.3

- 仅修复 Live 封面 `img` 切换为播放 `video` 节点后选框消失：同一 Live 宿主内的新媒体节点继承当前轮播页记录。
- Live 宿主仅用于确认节点归属，不参与选框尺寸和位置计算；MP4、普通图片/视频、Behance 和 ZIP 保持 1.8.2 不变。

## v1.8.2

- Live 选框不再绑定轮播外层容器，只在当前页稳定后按实际绘制画面显示一次，播放缩放不再带动选框和勾选图标。
- 修复多张 Live 流被合并成单个视频备用链的问题，按轮播页分组绑定真实 MP4，对应 WebP 封面不进入下载。
- Behance、ZIP、普通图片与普通视频逻辑保持 1.8.1 不变。

## v1.8.1

- 以 1.7.10 为基线重做，图片、普通视频和加入待下载恢复旧版流畅逻辑。
- 删除 1.8.0 的 Live 加载阻断和不可下载占位记录，识别到真实媒体后可直接加入。
- 普通图片只在真实 `naturalWidth/naturalHeight` 已产生时绘制实际图片选框，不再先画容器框。
- 普通图片轮播移动期间沿用旧版隐藏逻辑，停止后再按实际图片区域显示。
- 仅 Live 选框绑定当前页稳定外层容器；MP4 提取、Behance、ZIP 和其他站点保持不变。

## v1.7.10

- 按用户要求完整回退到 1.7.8 的运行代码，撤销 1.7.9 对选框显示时机、Live 首帧和页码缓存的修改。
- 除版本号和本条更新说明外，所有运行代码与 1.7.8 保持一致。

## v1.7.8

- 恢复已跑通的 Live MP4 导出链路，并为每个轮播页分别绑定真实视频流。
- Live 页面使用 `blob:` 播放时，从浏览器已加载的近期媒体资源恢复真实 MP4 地址。
- 确认为 Live 的轮播页会立即排除静态封面，不再将 WebP 封面伪装成 Live 下载结果。
- `xhs-content.js` 保持与 1.7.3 完全一致，Behance 与其他功能不变。

## v1.6.5

- 取消站酷搜索、推荐和收藏夹列表页的默认选择与火级筛选。
- 站酷改为仅在作品详情页采集；正文图片、GIF 和视频默认选中，并支持逐项取消或恢复。
- 视频按原视频格式加入 ZIP，不参与图片视觉哈希；GIF 保留原格式。
- 列表页提示进入作品详情，不再绘制选框，从根源上避免瀑布流错位。

## v1.6.4

- 修复站酷内部滚动容器滚动时选框不跟随、旧选框残留成竖线的问题。
- 所有滚动事件在捕获阶段监听，选框按动画帧同步，并自动删除已回收卡片的失效选框。
- 火级判断改为“火焰 SVG/图形造型＋推荐颜色/等级”，不再只凭红色或橙色判断。

## v1.6.3

- 站酷选中框改为直接跟随作品 `<img>` 的实际显示尺寸和位置。
- 不再覆盖站酷懒加载产生的灰色占位区域，未选中作品也不再添加灰色蒙层。
- 勾选按钮固定在实际作品图右上角，并在滚动和窗口缩放时同步定位。

## v1.6.2

- 兼容白色圆底＋小橙红火苗的站酷中火标记，默认选中。
- 增加背景图和伪元素火标识别，同时限定在封面右下角以避免误判普通图标。

## v1.6.1

- 站酷选中边框限定为 800×600（4:3）作品封面，不再包住标题和作者信息。
- 站酷选择状态移到封面右上角。
- 站酷默认仅选择中火、 大火作品；无火和小火作品默认不选，仍可手动恢复。

## v1.6.0

- 新增站酷搜索页、推荐列表、收藏夹和作品详情页采集。
- 列表页勾选作品卡片后，自动展开并采集作品详情正文中的全部图片。
- 使用 `站酷作品 ID + 图片 ID` 做历史去重，并接入跨平台视觉去重与混合 ZIP 队列。
- 排除作者头像、广告、相关推荐、评论图片和视频播放器；正文 GIF 按 GIF 文件保留。

## v1.5.4

- ZIP 内图片统一命名为 `0001-采集日期-图片原名.扩展名`，方便合并多次采集结果。
- 图片原名优先从原始图片 URL 或花瓣文件键提取，缺失时使用 Pin ID。

## v1.5.3

- 修复 ZIP 已保存但侧栏停在“正在打包 100%”、停止按钮失效的问题。
- 修复“花瓣 / Pinterest”汇总排序报错导致待下载按钮消失的问题。
- 下载改为跨平台统一队列，先采 Pinterest 再采花瓣（或反向）都能继续累积并一次打包。
- 侧栏重新加载时自动恢复 v1.5.2 遗留的打包卡死状态。

## v1.5.2

- 插件名称改为 `🌟 审美图采集器`。
- ZIP 统一命名为 `审美图-搜索名称-张数-日期.zip`。
- 最近采集按花瓣 / Pinterest 来源汇总，并保存可点击返回的最近采集网址。

## v1.5.1

- Pinterest 搜索页不再采集“相关搜索”推荐词卡片。
- 下载失败项保留在待下载队列，成功项进入历史，可直接再次点击下载重试失败项。
- 下载完成文案显示失败原因汇总；最近采集改为仅按“花瓣 / Pinterest”来源汇总。

## v1.5.0

- 复用现有右侧栏 UI，新增 Pinterest 自动识别与页面适配器。
- 支持 Pinterest 首页、搜索页、画板和 Pin 详情相关推荐的可见卡片选择及跨页面队列。
- Pinterest 使用 Pin ID 或图片签名去重，读取 `srcset` 最大图片，并接入现有 ZIP 下载流程。

## v1.4.13

- 修复关闭智能过滤后仅清除红色标记、卡片仍处于未选中置灰状态的问题。
- 关闭时直接恢复绿色选中框、勾选角标和选中计数，并兼容虚拟瀑布流重建的卡片。
- 阻止关闭开关前尚未完成的异步像素分析再次排除图片。

## v1.4.12

- 修复智能过滤关闭后版权、广告和透明素材仍被自动排除的问题。
- 关闭开关时立即恢复当前页面由智能过滤自动排除的图片；保留用户手动取消状态。
- 重新开启时对当前页面重新执行过滤规则。

## v1.4.11

- 修复右侧栏常驻后跨标签、跨页面仍向旧搜索页发送采集命令的问题。
- 活动标签和 SPA 路由变化后自动重新识别页面并清除旧错误，错误状态可立即恢复操作。
- 主色更新为 `#FF7694`。

## v1.4.10

- ZIP 命名改为 `花瓣-采集日期-搜索内容-采集张数.zip`，并自动清理文件名非法字符。
- 跨页面队列使用最近一次采集的搜索词或画板标题；分包下载追加部分序号。
- 主色更新为 `#FF5391`。

## v1.4.9

- 底部署名与版本号固定在侧栏视口最底部，不再跟随内容位置变化。
- 删除独立历史去重信息条，将统计区“重复跳过”直接改名为“历史去重”。

## v1.4.8

- 历史去重改为统计区下方单行信息，避免第四列挤压换行。
- 缩小开关、操作按钮、进度条和辅助按钮，统一为更精致的轻量尺寸。
- 主色调整为柔和浅莓粉；底部固定展示 `by 五月和小麦🌾` 与版本号。

## v1.4.7

- 修复清空待下载按钮被通用按钮宽度规则压缩成竖排的问题。
- 改为 Chrome 右侧边栏模式，点击扩展图标直接展开，并适配侧栏全高布局。
- 底部增加 `by vv` 标识。

## v1.4.6

- 弹窗改为白色主背景、浅灰边框和黑灰文字，仅操作按钮及状态强调保留粉红色。
- 统一所有任务状态下按钮高度；采集中隐藏打包下载，仅显示采集中与停止采集。

## v1.4.5

- 删除待下载素材卡片及全选开关，选中数量统一由头部页面状态展示。
- 智能过滤下方直接展示采集、打包按钮与任务进度，缩短弹窗操作路径。

## v1.4.4

- 将“全选 / 取消全选”两个按钮合并为与智能过滤一致的全选开关，并跟随页面选中状态同步。

## v1.4.3

- 修复统计区被运行时内联样式覆盖后纵向排列的问题，待下载、重复跳过、来自画板固定并排展示。

## v1.4.2

- 弹窗升级为蓝紫至浅蓝渐变侧边栏风格，重排智能过滤、待下载素材与操作区层级。
- “采集选中图片”和“打包下载”并排展示；全选操作移至待下载素材卡片内。
- 智能过滤说明精简为“排除版权素材、明显广告、透明素材”。
- 更新扩展图标，并同步更新目录、清单、弹窗和包内版本号。

## v1.4.1

- 修复搜索页将接口全集误当成用户勾选图片的问题；采集白名单现在严格来自可见瀑布流的勾选 Pin ID。
- 加强版权素材识别，采集前读取卡片及其有限父级内的“版权素材”标签并默认排除。
- 恢复下载前视觉去重，以 Pin ID 为第一层、保守感知哈希为第二层；不同 Pin 的近乎相同图片不重复写入 ZIP。

## v1.4.0

- 修复搜索接口单页返回 37 条时提前结束的问题，取消固定 50 页上限，并增加跨页 Pin ID 去重与重复页保护。
- 删除筛选结果中心及其入口，保留瀑布流过滤、选择、跨页面队列与批量下载核心能力。
- 新增版权素材硬过滤：识别结构化标记、描述和页面标签，命中后不请求原图且默认排除。

## v1.3.2

- 图片读取或解码失败时默认通过并保持勾选，避免网络问题造成误删。
- 筛选结果中心的通过图片增加“手动排除”，并同步取消瀑布流勾选及移出待下载队列。

## v1.3.1

- 修正发布包顶层目录名，使解压目录、ZIP 文件名、Manifest、package.json 和弹窗版本一致。

## v1.3.0

- 红色排除卡片固定显示叉号，花瓣瀑布流内不允许直接恢复。
- 广告内容默认直接排除。
- 重复只使用花瓣全局 `pin_id`，删除视觉相似哈希判断。
- 新增独立筛选结果页，分开展示通过与已排除图片，恢复操作集中在结果页。

## v1.2.2

- 将图片过滤原因标签从左上角移到左下角。

## v1.2.1

- 将花瓣图片卡片的选择勾/叉角标从右上角移到右下角。

## v1.2.0

- 扩展名称改为“花瓣采集器-v”。
- 红色过滤项在所有采集入口强制排除，不进入待下载队列。
- 删除最小尺寸和极端长宽比策略及其设置，避免误判完整长图。
- 总过滤开关开启时，明显广告直接排除。
- 增加 ZIP 写入前的实际文件精确哈希与感知哈希二次去重。

## v1.1.0（历史版本）

- 增加采集前过滤：小图、极端比例、透明单体、视觉重复和疑似广告。
- 页面使用红色/黄色状态和原因标签，所有自动过滤结果均可人工恢复。
- 过滤阈值可在扩展弹窗中配置，并可一键重新过滤。
- 当时的广告规则默认只标记为疑似；此行为已在 v1.2.0 调整。

## v1.0.0

### English

- Board, search, pin-detail, and recommendation collection flows.
- SPA navigation detection with API fallbacks.
- Persistent cross-page queue and two-stage deduplication.
- Six-request concurrency, chunked ZIP output, pause, and resume.
- Public-release documentation and task-store tests.

### 中文

- 支持画板、搜索、Pin 详情和推荐流采集。
- 支持 SPA 导航检测与 API 兜底。
- 提供跨页面持久队列和双阶段去重。
- 提供六路并发、分批 ZIP、暂停和恢复。
- 增加公开发布文档和任务存储测试。
# 1.6.6

- 站酷选框改为正文层级，不再压住固定导航或图片预览弹层。
- 站酷打开图片预览/放大时自动隐藏采集选框，关闭后恢复。
- 选框按 `object-fit` / `object-position` 计算图片真实绘制区域，不再包含 `contain` 留白。
- ZIP 内媒体统一为 `0001-采集日期-名称.扩展名`；站酷使用作品名，花瓣和 Pinterest 保留各自原始图片名。
# 1.6.7

- 站酷选框改为绑定媒体容器的绝对定位层，随页面同步滚动，不再出现追帧飞移。
- 站酷正文图识别不再强依赖“（图x）” alt，增加正文容器与尺寸识别。
- 新增头像、作者、评论、推荐、图标和 Logo 排除，降低放宽识别后的误采。
- 站酷勾选按钮移到图片右下角，与花瓣 Pin 共用同一套位置、圆形样式和勾/叉图标。
- 移除滚动期间的 JavaScript 坐标追踪；补充站酷 CDN 子域名和懒加载尺寸字段识别。
# 1.6.8

- 站酷作品页头部横幅广告不再因大尺寸兜底而进入采集队列。
- 采集长作品时重读完整作品 HTML，补齐尚未渲染到 DOM 的懒加载正文图。
- 站酷选框改用与花瓣、Pinterest 相同的 `huaban-dl-card + huaban-dl-overlay + huaban-dl-badge` 结构和状态样式。
# 1.6.9

- 站酷作品正文仅以站酷“（图ID）”标记或已加载的完整作品目录匹配，彻底排除头部广告、相关推荐和评论区图片。
- 进入作品页即预读完整正文图目录，侧栏总数和采集不再受懒加载滚动位置影响。
- 后半部正文图尚未渲染时也会进入采集队列，渲染后自动补上选框。
# 1.6.10

- 站酷懒加载识别改为 50ms 固定短节流，连续滚动时也会持续完成扫描。
- 监听 `src` / `srcset` / `data-src` 等属性更新和媒体 `load` 事件，已存在节点的懒加载图也能立即补选框。
- 完整正文目录增加规范化 URL 索引，媒体节点匹配由逐项遍历改为直接查找。
- 侧栏新增持久化“插件开关”；关闭时三个网站立即隐藏选框、停止新扫描并禁止采集，重新开启后自动恢复。
# 1.6.11

- 花瓣版权/疑似素材仍默认排除，但用户可直接点击叉号手动恢复勾选；手动选择优先于自动分类。
- ZIP 内统一为 `0001-采集日期-来源-图片信息.扩展名`。
- 来源字段分别为“花瓣”、“Pinterest”和“站酷”；站酷图片信息固定使用作品名称。
# 1.6.12

- 侧栏选择数和过滤统计仅在实际状态变化时更新，消除滚动时的重复重绘抖动。
- 花瓣视觉状态为绿色选中、红色明确排除、黄色疑似（默认不采集）、灰色用户取消；红色和黄色均可手动恢复勾选，侧栏单独统计疑似。
- 花瓣版权识别新增 PSD 素材、PSD 源文件、正版/付费/可商用素材及卡片属性标记扫描。
- 站酷作品页新增独立的整件取消/重新全选按钮。
- 固定底栏中间新增“意见反馈”飞书表单入口。
- 智能过滤统计中的“正常 / 疑似 / 排除”前增加绿色、黄色、红色状态圆点。
# 1.6.13

- 智能过滤统计改为按当前界面状态实时重算，手动恢复、单张取消和整批操作会立即反映到数值。
- 统计行新增灰色圆点“手动排除”，与绿色正常、黄色疑似、红色排除分开计数。

# 1.6.14

- 站酷取消本作品全部选择时，同步撤回该作品已经加入待下载队列的全部图片与视频。
- 本作品选中数为零时禁用采集按钮，并用明确状态提示替代“未找到正文图片”的误报。

# 1.6.15

- 花瓣与 Pinterest 在图片已经加入待下载后，用户手动排除会立即从待下载队列撤回对应图片。
- 花瓣后续识别为版权、广告或疑似素材时继续同步撤回；Pinterest 后续识别为推广内容时同样撤回。
- 待下载数量会在撤回后实时刷新，保留“页面预选 → 确认采集 → 打包下载”的安全流程。

# 1.6.16

- 底栏意见反馈更新为新的飞书表单，文案改为“点击此处反馈意见”并与左右信息垂直对齐、水平居中。
- 花瓣与 Pinterest 列表页主按钮由“采集选中图片”改为“加入待下载”，明确页面预选与待下载队列的关系。

# 1.6.17

- 底栏调整为“作者｜点击此处反馈意见”左侧组合，版本号保持右对齐。
- 花瓣与 Pinterest 已从待下载手动排除的图片，重新勾选后会恢复到原待下载队列。
- 后台返回实际移除的图片 ID，避免从未入队的图片在勾选时被错误加入。

# 1.6.18

- 新增小红书笔记详情页采集：图文笔记识别完整图集，视频笔记识别视频媒体。
- 小红书首页和列表页不采集，必须点开具体笔记后才显示选框并允许加入待下载。
- 小红书支持单项取消/恢复及整篇取消/重新选择，已入队内容会同步撤回或恢复。
- 小红书导出命名为 `0001-采集日期-小红书-笔记标题`，并纳入跨站待下载统计与去重。

# 1.6.19

- 小红书选框改为根据原图宽高、CSS `object-fit` 与 `object-position` 计算实际绘制边界，不再框住侧边栏挤压后的整个轮播容器。
- 选中角标跟随真实图片右下角，侧边栏开合或窗口尺寸变化后实时重新定位。
- 小红书优先使用笔记数据中的原始图片地址并移除 CDN 展示缩放参数，下载不再沿用当前展示框尺寸。

# 1.6.20

- 小红书只为当前可见轮播媒体显示一个选框，排除模糊背景、预加载轮播项和隐藏副本，修复多层重叠绿框。
- 小红书选框改为独立固定绘制层，不再给站点父容器添加定位样式，修复展开侧边栏后图片错位。
- 增加轮播页码识别；当最后一张图片的 CDN 地址与笔记数据不一致时，按当前页码补齐对应媒体。

# 1.6.21

- 小红书轮播选框改为直接绘制在当前图片或视频元素上，与轮播共用浏览器合成层，修复滑动时选框延迟和重影。
- 站酷与小红书详情页主按钮统一为“加入待下载”，整件取消按钮统一为“取消选择此作品的全部图片”。
- 小红书下载继续优先使用原始媒体地址，并移除 CDN 展示缩放与水印处理参数；不处理创作者写入原图像素的署名。
- ZIP 总包统一命名为“审美图-采集日期-张数.zip”。

# 1.6.22

- 小红书在页面滚动、侧栏伸缩或轮播滑动期间立即隐藏选框与勾选图标，画面连续稳定两帧后再定位到当前媒体，消除滑动过程中的飞框和乱跳。
- 回归确认 GIF 会按原始 `image/gif` 文件写入 ZIP；小红书和站酷的视频会按 MP4/WebM 等原媒体格式加入待下载。

# 1.6.23

- 小红书轮播的绿色容器选框改为常驻，滚动或滑动时只隐藏右下角勾选图标；媒体进入评论区以外的不可见层后图标彻底隐藏，不再吸附到窗口底部。
- 小红书视频元素提高识别优先级并可直接加入待下载；Live Photo 媒体从笔记数据中提取视频流并转换为 GIF。
- 原始 GIF 直接保留动画，动态 WebP/AVIF 在浏览器支持 `ImageDecoder` 时转换为 GIF。
- 取消 ZIP 打包，所有文件逐个保存到浏览器默认下载目录下的“审美图-采集日期-张数”文件夹。

# 1.6.24

- 点击下载后先打开系统文件夹选择器，由用户明确选择保存位置，再在所选位置创建本次采集文件夹。
- 视频笔记不再把封面静态图当作正文媒体；当前封面或视频元素统一绑定到原视频记录并显示绿色选框。
- 小红书 Live 媒体不再转换 GIF，直接保存平台提供的动态视频流为 MP4；原生 Live Photo 配对元数据无法从小红书恢复。

# 1.6.25

- 视频笔记选框绑定到与视频同尺寸的播放器外层，避免被小红书自定义控制层遮住。
- Live Photo 勾选图标改用轮播可见性判断，不再因 LIVE/轮播覆盖层抢占命中而消失。
- 视频笔记仅保留一个真实主视频媒体，不再把封面或重复视频流加入待下载。
- 导出恢复为单个 ZIP，只弹出一次标准“另存为”，避免 Chrome 对系统或敏感目录的文件夹权限提示。

# 1.6.26

- Live Photo 选框改为按原图真实 `object-fit` 绘制边界，不再泄漏到轮播留白或页码区域。
- 扩展 Live/Motion Photo 字段识别，并为主地址保留小红书备用视频流，首个地址失效时自动切换。
- 导出前检查 MP4/WebM 文件头；如果平台返回静态 JPEG/WEBP 封面，则继续尝试备用流，不再把静态图伪装成 MP4。
- 视频地址按 MP4、原始/master 和 H.264 优先级排序，视频笔记仍只保留一个真实主视频。

# 1.6.27

- 参考好采助手 0.1.2 的本地媒体兜底思路：初始状态未提供视频时，继续从 `og:video`、当前笔记 HTML 的 `masterUrl` 和 DOM `video/source` 中提取。
- 识别 `.live-photo-contain` 内的 Live 静态封面；未取得真实动态流时不再降级为普通图片入队。
- 多条 MP4 流时参考稳定版本后缀优先级，优先 `_114.mp4`、`_115.mp4`，再使用其他 master/H.264 流。
- 未移植参考插件的浏览器指纹、Eagle/Feishu 远程接口、密钥保存或额外权限。

# 1.7.0

- Live 开始播放并在封面 `img`/动态 `video` 之间切换节点时，按同一个逻辑媒体处理，不再重置选框稳定状态。
- Live 选框始终使用 fixed 精确边界，不在播放期间切换为视频容器 outline。
- Live 视频记录继承对应封面的原始宽高，勾选图标和选框共用同一稳定绘制区域，不再跑到轮播页码或画框之外。
- 版本号从本版开始按每段 0–10 递增；超过 10 时进位前一段。

# 1.7.1

- Live 动图首次识别后锁定选框尺寸；播放节点自动放大时只跟随中心位置，不再同步放大或隐藏选框。
- 下载按钮文案改为“下载 X 项到本地”。
- 绿色选中状态移除半透明填充，仅保留绿色外框和勾选图标；其他颜色状态保持不变。

# 1.7.2

- Live 选框从“只锁定首次宽高”改为“锁定首次完整矩形”，播放节点缩放或切换时不再读取漂移的中心坐标。
- 页面真实滚动时仅按滚动差值平移选框，Live 播放动画本身不再带动选框和勾选图标。

# 1.7.3

- 关闭小红书笔记详情后，在切回列表 URL 的下一动画帧立即清除选框、勾选图标和旧笔记选择状态。
- 清理不再等待 DOM 观察或一秒定时扫描，避免 fixed 选框短暂残留在搜索结果页。

# 1.7.4

- Live 第一次打开时即使用封面元数据创建临时选框；真实视频流稍后到达后自动替换，临时记录不会进入下载队列。
- 新增 Behance 项目详情页采集：仅识别 `/project_modules/` 正文图片，排除列表页、头像和推荐项目封面。
- Behance 图片优先请求 `project_modules/source` 原图，失败时回退页面展示地址，并接入选框、整件选择、待下载、去重和 ZIP 导出。

# 1.7.5

- 小红书媒体候选限制为当前详情弹层且必须匹配当前笔记，禁止首次打开时误锁底层瀑布流图片。
- Live 视频兜底地址在首次过早解析为空时按节流间隔自动重试，真实流到达后可正常加入待下载。
- Behance 大图改用缩略尺寸解码生成视觉哈希，媒体请求增加 20 秒超时，图片在 ZIP 中使用 STORE，避免下载与打包长期卡住。

# 1.7.6

- 保留 Behance 项目详情页识别、原图回退、待下载与不卡顿 ZIP 导出功能。
- 小红书 `xhs-content.js` 完整回退为 1.7.3，选框、勾选图标、定位和切页行为与 1.7.3 完全一致。
- 仅在数据解析层保留 Live 首次打开修复：视频兜底首次为空时每 1.2 秒重试，取得真实视频流后停止。

# 1.7.7

- 根据录屏确认“第1张有框、后续页无框、返回第1张恢复”是Live流只绑定首个轮播页导致。
- 每个Live页首次被切到时，按当前页码缓存该页可见视频流并发布对应 `slideIndex`，后续页无需关闭重开即可识别。
- 小红书选框文件继续与1.7.3完全一致；Behance及其他已正常功能保持不变。
